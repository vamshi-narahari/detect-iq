VITE_SUPABASE_URL=https://gckbdtcguptlfulzekzx.supabase.co
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=/api

# Optional: powers the "Try the demo — no signup" button. Create one real
# account for this (e.g. demo@detect-iq.com) and put its credentials here.
# It's fine for these to be visible in the browser bundle — that's the point,
# it's meant to be a public shared demo login.
VITE_DEMO_EMAIL=
VITE_DEMO_PASSWORD=
